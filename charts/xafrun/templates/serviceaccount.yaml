{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "xafrun.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "xafrun.labels" . | nindent 4 }}
{{- end }}
